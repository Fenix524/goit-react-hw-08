import css from './NotFoundPage.module.css'

const NotFoundPage = () => {
	return (
		<div className={css.NotFoundPage}>
			Oops, we couldn't open this page, it may be out of date!
		</div>
	)
}

export default NotFoundPage
